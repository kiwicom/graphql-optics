{
 allLocations(search: "Boulder") {
   edges {
     node {
       locationId
       name
       slug
       timezone
       type
       isActive
       stationsCount
       airportsCount
       rank
     }
   }
 }
}
